# دليل اختبار نظام الترسية الجزئية

## ✅ الميزة: التحكم في الترسية (جزئية أم كاملة)

### المتطلب:
- عند إنشاء المناقصة، يختار المشتري نوع الترسية
- الترسية الكاملة (Full Award): فائز واحد فقط
- الترسية الجزئية (Partial Award): عدة فائزين

---

## السيناريو 1: ترسية كاملة (ترسية كاملة)

### عند الإنشاء:
```
المشتري ينشئ مناقصة:
☐ السماح بالترسية الجزئية
∧ عدد الفائزين: 1 (بشكل افتراضي)

الحد الأقصى للفائزين: 1 فائز فقط
```

### عند الاختيار:
```
✓ يمكن اختيار فائز واحد
✗ لا يمكن اختيار أكثر من فائز

إذا حاول اختيار 2 فائزين:
❌ خطأ: "هذه المناقصة تسمح بفائز واحد فقط"
```

### التحقق من قاعدة البيانات:
```sql
SELECT allow_partial_award, max_winners FROM tenders WHERE id = [tender_id];
-- النتيجة: allow_partial_award = FALSE, max_winners = 1
```

---

## السيناريو 2: ترسية جزئية (Partial Award)

### عند الإنشاء:
```
المشتري ينشئ مناقصة:
☑ السماح بالترسية الجزئية
∧ عدد الفائزين: 3

يمكن تقسيم المناقصة على عدة مزودين (حد أقصى 3)
```

### عند الاختيار:
```
✓ يمكن اختيار فائز واحد
✓ يمكن اختيار فائزين
✓ يمكن اختيار 3 فائزين
✗ لا يمكن اختيار 4 فائزين

إذا حاول اختيار 4 فائزين:
❌ خطأ: "الحد الأقصى هو 3 فائزين. اخترت 4"
```

### التحقق من قاعدة البيانات:
```sql
SELECT allow_partial_award, max_winners FROM tenders WHERE id = [tender_id];
-- النتيجة: allow_partial_award = TRUE, max_winners = 3
```

---

## ✅ حالات الاختبار

| الحالة | الإنشاء | الاختيار | النتيجة |
|--------|--------|----------|--------|
| ترسية كاملة | `partial = FALSE` | 1 فائز ✓ | نجح |
| ترسية كاملة | `partial = FALSE` | 2 فائز ✗ | خطأ |
| ترسية جزئية | `partial = TRUE` | 1-3 فائز ✓ | نجح |
| ترسية جزئية | `partial = TRUE` | 4+ فائز ✗ | خطأ |

---

## 🔍 التحقق من التطبيق

### 1. عند الإنشاء:
```
✓ حقل "السماح بالترسية الجزئية" موجود
✓ حقل "عدد الفائزين" يظهر حسب الاختيار
✓ القيم تُحفظ في قاعدة البيانات
```

### 2. عند الترسية:
```
✓ الفائزين المختارين يفي بالقيود
✓ رسالة خطأ واضحة عند الانتهاك
✓ الترسية تُنجح عند الامتثال
```

### 3. في قاعدة البيانات:
```
✓ allow_partial_award = FALSE/TRUE
✓ max_winners = العدد الصحيح
✓ جميع الترسيات تمتثل للقواعد
```

---

## 📋 قائمة التحقق

- [ ] حقل الترسية الجزئية يظهر في الإنشاء
- [ ] حقل عدد الفائزين يظهر حسب الاختيار
- [ ] القيم الافتراضية صحيحة
- [ ] الفائز الواحد يعمل دائماً
- [ ] عدة فائزين ممنوع في الترسية الكاملة
- [ ] عدة فائزين مسموح في الترسية الجزئية
- [ ] الحد الأقصى يُفرض بشكل صحيح
- [ ] رسائل الخطأ واضحة ومفيدة
- [ ] قاعدة البيانات تحتوي على القيم الصحيحة

